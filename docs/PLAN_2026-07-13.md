# แผนปรับปรุง & เตรียมขึ้น Production — Project Document (Document ES)

> จัดทำ 2026-07-13 · ตรวจสอบจากโค้ดจริง (ไม่ใช่แค่เอกสารเดิม) · `go build ./...` และ `tsc --noEmit` = 0 error

## สรุปภาพรวม
โค้ด **พร้อมระดับ production เป็นส่วนใหญ่แล้ว** — ไม่มี blocker ที่ทำให้ระบบใช้งานไม่ได้
สิ่งที่ทำไว้ดี (ยืนยันจากโค้ด): graceful shutdown, HTTP timeouts, pgxpool tuning, CORS env-gated,
storage factory, parameterized SQL ทั้งหมด, RBAC 2 ชั้น, JWT refresh rotation (hash+revoke),
path ไฟล์เป็น UUID (กัน traversal), SSE + token-rotation reconnect, FK ON DELETE CASCADE,
Acknowledge race กันด้วย unique index + ON CONFLICT, soft-delete user + revoke session

งานที่เหลือ = **hardening (โค้ด)** + **infra/config ตอน deploy**

---

## 🔴 กลุ่ม A — Hardening ก่อนขึ้น production (งานโค้ด)

| # | เรื่อง | จุดในโค้ด | แนวทาง | สถานะ |
|---|---|---|---|---|
| A1 | ไม่มี Rate Limiting | `/auth/login`, `/auth/refresh`, `/auth/azure/exchange` | `middleware/ratelimit.go` token-bucket ต่อ IP (30/นาที, burst 10) | ✅ ทดสอบ runtime: 429 หลัง burst |
| A2 | ไม่มี Security Headers | `middleware/security.go` + `next.config.mjs` headers() | X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy | ✅ ทดสอบ runtime: header ครบ |
| A3 | Upload validate แค่ client + Content-Disposition ดิบ | `document.go` `validateUpload`/`contentDisposition` | whitelist ext (pdf/jpg/jpeg/png) ฝั่ง server + sanitize header (RFC 5987) | ✅ build ผ่าน |
| A4 | ไม่มี Error Boundary | `app/error.tsx` + `app/global-error.tsx` | fallback ภาษาไทย กันจอขาว | ✅ build ผ่าน |
| A5 | Next.js 14.2.5 มี CVE สะสม | `package.json` | อัปเป็น **14.2.35** (patch สาย 14.2 — ไม่ breaking) | ✅ ติดตั้ง+build ผ่าน |
| A6 | SSE โดน WriteTimeout 5 นาที ตัด | `notifications.go` Stream | `http.NewResponseController(w).SetWriteDeadline(time.Time{})` | ✅ build ผ่าน |

> CVE Next ที่ไม่กระทบ: i18n / middleware / beforeInteractive / CSP-nonce (แอปไม่ได้ใช้ — ไม่มี `middleware.ts`, ไม่มี i18n)
> **หมายเหตุ A5:** อัป 14.2.35 เก็บ patch ทั้งหมดของสาย 14.2 แล้ว แต่ `npm audit` **ยังไม่ 0** — เหลือ advisory กลุ่ม DoS/RSC/rewrite-smuggling ที่ Vercel patch เฉพาะใน Next 15/16 (major breaking) ส่วนใหญ่ไม่กระทบการใช้งานจริง (image ใช้แค่ avatar ภายใน) — การอัป Next 16 เป็นการตัดสินใจแยก **อย่า** `audit fix --force`

## 🟠 กลุ่ม B — Infra / Config ตอน Deploy
อ้างอิงหลักที่ `docs/DEPLOY_CHECKLIST.md` (TLS, Azure redirect URI, binary+process manager, nginx SSE, backup) + เพิ่มที่พบใหม่:
- **Frontend Dockerfile ขาด `NEXT_PUBLIC_BASE_PATH` ARG** (ส่งแค่ API_BASE_URL) → build ไม่ติด basePath
- **Docker ไม่มี HEALTHCHECK** ทั้ง backend/frontend (backend มี `/healthz` ใช้ได้เลย)
- **docker-compose ตั้ง JWT secret default อ่อน** (`dev-...-change-me`) → prod ต้อง require env จริง (ตัด `:-default`)
- **avatars volume ไม่ถูก mount** ใน compose (`storage/avatars` หายเมื่อ rebuild) → เพิ่ม volume
- backend Dockerfile รัน `go mod tidy` ตอน build + รันเป็น root → pin deps + non-root user
- `.env.local.example` ไม่ครบ (ขาด `BACKEND_URL`, `NEXT_PUBLIC_BASE_PATH`)
- ตั้ง log rotation สำหรับ prod

## 🟢 กลุ่ม C — ประสิทธิภาพ/ใช้งานระยะยาว (หลัง go-live)
- Documents/Reports fetch `size:500` แล้ว slice ฝั่ง client → ทำ backend pagination จริง (backend รองรับ page/size อยู่แล้ว) — ⏳ stopgap แล้ว (ดูบันทึก 2026-07-17)
- ย้าย refresh token → httpOnly cookie (ลด XSS) — ทำคู่กับ refresh token rotation (ต้องออกแบบ multi-tab grace window)
- ~~รวม query dashboard~~ ✅ 2026-07-17
- Multi-user acknowledge (ถ้าธุรกิจต้องการ — ต้องแก้ schema)
- ~~aging ใช้ server date แทนนาฬิกา client~~ ✅ 2026-07-17

---

## ลำดับการทำงาน
1. **Phase 1** — กลุ่ม A ทั้งหมด (งานโค้ด, ทำบนเครื่อง dev, build ต้องผ่าน)
2. **Phase 2** — กลุ่ม B (ตอนเตรียม prod: env, secret ใหม่, DB จริง, proxy, process manager)
3. **Phase 3** — Verify ตาม DEPLOY_CHECKLIST ข้อ 6
4. **Phase 4** — กลุ่ม C ตามการเติบโตของข้อมูล

## บันทึกการทำ Phase 1 — เสร็จ 2026-07-13 ✅
กลุ่ม A ครบทั้ง 6 ข้อ · `go build ./...` + `go vet` = 0 · `tsc --noEmit` + `npm run build` (12/12 routes) = 0
ทดสอบ runtime (backend restart): security headers ครบทุก response + rate limit คืน 429 หลัง burst

**ไฟล์ที่เพิ่ม/แก้:**
- เพิ่ม: `backend/middleware/security.go`, `backend/middleware/ratelimit.go`, `frontend/src/app/error.tsx`, `frontend/src/app/global-error.tsx`
- แก้: `backend/cmd/server/main.go` (wire 2 middleware), `backend/handlers/document.go` (validateUpload/contentDisposition), `backend/handlers/notifications.go` (SSE deadline), `frontend/next.config.mjs` (headers), `frontend/package.json` (next 14.2.35)

**ต่อไป:** Phase 2 (กลุ่ม B) เมื่อเตรียมขึ้น server จริง

## บันทึก Security fixes รอบ 2 — 2026-07-13 ✅ (commit ad78d52)
จาก security review พบ+แก้เพิ่ม 4 ข้อ (ทดสอบ runtime ผ่านครบ):
1. เอา `server.log`/`dev.log`/`tsconfig.tsbuildinfo`/รูป avatar ออกจาก git (server.log มี access_token จาก SSE query — token 15 นาทีหมดอายุแล้ว แต่ history เก่ายังมีไฟล์อยู่ ถ้าจะล้างจริงต้อง rewrite history)
2. `middleware/logger.go` — RedactedLogger scrub `access_token=` ใน log (แทน gin.Default)
3. `TRUSTED_PROXIES` env + `SetTrustedProxies` — กันปลอม X-Forwarded-For หลบ rate limit; **prod ต้องตั้ง `TRUSTED_PROXIES=127.0.0.1`** (same-origin ผ่าน Next proxy) ไม่งั้นทุกคนแชร์ bucket เดียว → 429
4. docker-compose ตัด default JWT secret (`:?` fail-fast) + parametrize `POSTGRES_PASSWORD`

**คงเหลือ (medium):** refresh token rotation ยังไม่มี (Refresh ออกแค่ access ใหม่), token ใน localStorage (กลุ่ม C), ยังไม่มี CSP header
**การตัดสินใจ:** สิทธิ์ดาวน์โหลดเอกสาร = ทุกคนในองค์กรโหลดได้ (org-wide) **ยืนยันเป็นดีไซน์แล้ว 2026-07-13** — ไม่ต้องเพิ่มเช็คระดับแผนก

## บันทึกการทำ Phase 2 (กลุ่ม B ส่วนโค้ด/config) — 2026-07-14 ✅
ทำครบ 6 ข้อ เรียงตามความร้ายแรง · ทดสอบด้วย docker build + รัน container จริงทุก image:
1. **avatars volume** — เพิ่ม `pd_avatars:/app/storage/avatars` ใน compose (กันรูปหายตอน rebuild) · ทดสอบเขียนไฟล์ใน volume ผ่าน
2. **backend Dockerfile** — `go mod download` จาก go.mod+go.sum แทน `go mod tidy` (pin deps), รันเป็น non-root user `app`, pre-create storage dirs ให้ volume inherit ownership, เพิ่ม HEALTHCHECK `/healthz` · container health=healthy
3. **frontend Dockerfile** — เพิ่ม ARG `NEXT_PUBLIC_BASE_PATH` + `BACKEND_URL` (ทั้งคู่ถูก bake ตอน build ผ่าน next.config.mjs), เพิ่ม HEALTHCHECK, **แก้บั๊กเดิม 2 จุดที่ทำให้ image ใช้ไม่ได้มาตลอด**: `COPY /app/public` ล้ม (โปรเจกต์ไม่มีโฟลเดอร์ public) และ Next standalone bind ตาม container hostname → เพิ่ม `ENV HOSTNAME=0.0.0.0` · container health=healthy
4. **frontend/.dockerignore** — ใหม่ กัน node_modules/.next จากเครื่อง dev ปนเข้า image
5. **compose** — frontend รอ backend `service_healthy`, ส่ง build args ครบ
6. **.env.local.example** — เติม `BACKEND_URL`, `NEXT_PUBLIC_BASE_PATH` พร้อมคำอธิบาย build-time

**คงเหลือกลุ่ม B (ทำตอน deploy จริง):** log rotation, TLS/proxy/backup ตาม `docs/DEPLOY_CHECKLIST.md`
**หมายเหตุ:** volume `pd_documents` ที่สร้างด้วย image เก่า (root) — ถ้า container ใหม่ (non-root) เขียนไม่ได้ ให้ `chown -R 100:101` ใน volume หรือสร้าง volume ใหม่

## บันทึกการทำกลุ่ม C (ส่วนที่ทำได้ทันที) — 2026-07-17 ✅
วิเคราะห์ผลกระทบก่อนทำ แล้วแยกเป็น "ทำเลย" กับ "รอ":
1. **รวม query dashboard** ✅ — `GET /dashboard` เดิมยิง 10 query/โหลด → เหลือ **3**: KPI ทั้งหมด (total/mine/pending_ack/acked_today/trend/statuses) รวมเป็น 1 scan ของ `documents` (FILTER + scalar subquery), activity 3 แหล่ง (upload/ack/edit) รวมเป็น UNION ALL เดียว (แต่ละ branch เก็บ ORDER BY/LIMIT ของตัวเองไว้ให้ใช้ index เดิม), daily คงเดิม · response shape เดิมเป๊ะ · ตรวจ SQL เก่า-ใหม่บนข้อมูลจริง = ตรงกันทุกค่า · ทดสอบ runtime หลัง restart ผ่าน
2. **aging ใช้ server date** ✅ — `List()` (response.go) แนบ `meta.server_date` (YYYY-MM-DD, server-local) ทุก list response; frontend เพิ่ม `serverToday()` ใน `lib/utils.ts` แล้วใช้แทน `new Date()` ในทุกจุดนับวัน: reports `computeAging` + preset ช่วงวันที่, dashboard `KpiCheckModal` (รับทราบวันนี้/รอรับทราบนานสุด) + `ActionQueue`
3. **กันข้อมูลหายเมื่อเกิน 500 แถว (stopgap ของ pagination)** ✅ — เพิ่ม `documentApi.listAll()` วนดึงทุกหน้า (500/หน้า จนครบ `meta.total`) แล้วเปลี่ยน 4 จุดที่ใช้ cache `["documents","all"]` (documents/reports/dashboard/profile) มาใช้แทน `list({size:500})` — filter/sort/export ฝั่ง client ทำงานเหมือนเดิมแต่ไม่ truncate เงียบๆ อีกต่อไป

**ตัดสินใจ "รอ" พร้อมเหตุผล:**
- **Backend pagination เต็มรูปแบบ** — ต้องย้าย per-column filter (distinct values), sort, KPI modal, export ไป server ทั้งหมด = redesign ใหญ่; ข้อมูลจริงตอนนี้ ~26 แถว ยังไม่คุ้ม ทำเมื่อแตะหลักพัน
- **httpOnly cookie + refresh rotation** — ต้องทำคู่กัน และมี race หลายแท็บ (แท็บ A rotate แล้วแท็บ B ใช้ token เก่า → โดน revoke ทั้งคู่) ต้องออกแบบ grace window ก่อน
- **Multi-user acknowledge** — รอธุรกิจยืนยัน (ต้องแก้ schema + UNIQUE index ของ acknowledgements)
