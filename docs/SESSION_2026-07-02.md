# Session Summary — 2026-07-02

## 🔐 Infrastructure & Deployment

### HTTPS + Web Crypto
- เปิดใช้ `next dev --experimental-https` ให้ frontend รันบน HTTPS
- ติดตั้ง **mkcert** rootCA เข้า Windows trust store (admin)
- Generate cert ครอบคลุม `localhost`, `127.0.0.1`, `10.51.192.198`, `::1`
- ทำให้ `window.crypto.subtle` (Web Crypto API) ทำงาน → **MSAL/Azure login ใช้งานได้**

### Next.js API Proxy
- `next.config.mjs` เพิ่ม rewrites:
  - `/api/v1/*` → `http://localhost:8080/api/v1/*`
  - `/uploads/*` → `http://localhost:8080/uploads/*`
- Frontend เรียก **same-origin** → หมดปัญหา CORS
- Backend ยัง HTTP ปกติ ไม่ต้องแก้

### basePath `/Document`
- Env: `NEXT_PUBLIC_BASE_PATH=/Document`
- ทุก route auto-prefix: `/Document/login`, `/Document/dashboard`, `/Document/documents` ฯลฯ
- API client + SSE + MSAL redirect URI ทุกจุด prepend basePath อัตโนมัติ
- **Azure Portal** — ต้อง add SPA URI ใหม่: `https://IP:3000/Document/login`

### Backend CORS
- ปรับ `middleware/cors.go` — accept origin ที่เป็น LAN IP (10.x, 192.168.x, 172.16-31.x, localhost, 127.x) อัตโนมัติ
- ไม่ต้อง hard-code IP ทุกเครื่อง

---

## 🗄 Backend — Migration 014 + Handlers

### Migration `014_staff_project_type.up.sql`
```sql
-- Staff Master (แยกจาก users)
CREATE TABLE staff_master (
    id UUID PRIMARY KEY,
    employee_id VARCHAR(64) UNIQUE NOT NULL,
    full_name VARCHAR(255) NOT NULL,
    department_id UUID REFERENCES departments(id),
    position_id UUID REFERENCES positions(id),
    is_active BOOLEAN DEFAULT TRUE,
    created_at, updated_at TIMESTAMPTZ
);

-- Documents
ALTER TABLE documents
  ADD COLUMN project_type VARCHAR(16) DEFAULT 'Standard',
  ADD COLUMN owner_project_staff_id UUID REFERENCES staff_master(id);

-- Revert UAT/UAI status defaults
ALTER COLUMN uat_status SET DEFAULT 'Pending';
ALTER COLUMN uai_status SET DEFAULT 'Pending';

-- Data migrate
UPDATE documents SET uat_status = 'Pending' WHERE uat_status IN ('Standard','Modify','Add-on');
UPDATE documents SET uai_status = 'Pending' WHERE uai_status IN ('Standard','Modify','Add-on');
UPDATE documents SET owner_project_user_id = NULL;
```

### Handlers ใหม่

**`handlers/staff.go`** — CRUD Staff:
| Method | Path | Access |
|---|---|---|
| GET | `/api/v1/staff` | ทุก user (dropdown) |
| GET | `/api/v1/admin/staff` | admin (รวม inactive) |
| POST | `/api/v1/admin/staff` | admin |
| PATCH | `/api/v1/admin/staff/:id` | admin |
| DELETE | `/api/v1/admin/staff/:id` | admin (soft delete) |

### Handlers แก้ (`document.go`)

**`normStatus()` revert:**
```go
case "passed": return "Passed"
case "failed": return "Failed"
case "pending", "": return "Pending"
case "standard", "modify", "add-on", "addon": return "Pending"  // legacy fallback
```

**`normProjectType()` ใหม่:**
```go
case "modify": return "Modify"
case "add-on", "addon": return "Add-on"
default: return "Standard"
```

**`Create` handler:**
- รับ `project_type` (auto default Standard)
- รับ `owner_project_staff_id` (**required** — 422 ถ้าไม่ส่ง)
- รับ `files[]` (multi-file) เก็บเป็น `kind='ATTACHMENT'`
- Backward compat: `file_uat`, `file_uai`, `file` (single) ยังรับได้

**`Detail`:**
- Join `staff_master` แทน `users` สำหรับ owner_project_name
- Return: `project_type`, `owner_project_name`, `owner_project_employee_id`

**`List`:**
- เพิ่ม `project_type`, `files_count` ใน response

---

## 🎨 Frontend — Upload Rewrite + Staff Tab

### Upload Page (`documents/upload`) — 3 sections

#### Section 1: ข้อมูล Project
- 🆕 **Dropdown "ประเภท (Type)"** — Standard / Modify / Add-on **required**
- **ชื่อบริษัท** *
- **WorkOrder** *
- 🆕 **ผู้รับผิดชอบ Project** — Dropdown ดึงจาก `/staff` (แทน users) **required**
- **วันที่ติดตั้ง** (optional)
- **หมายเหตุ** (optional)

#### Section 2: สถานะ UAT / UAI
- 🔄 ปุ่ม status กลับเป็น **Pending / Passed / Failed**
- สี: slate (Pending) / emerald (Passed) / rose (Failed)

#### Section 3: ไฟล์แนบ (🆕 unified)
- **Drag & Drop zone** เดียว (ตัด UAT/UAI file split ทิ้ง)
- คลิกเลือกไฟล์หรือลากมาวาง
- รองรับ **PDF, JPG, PNG** ≤ **10 MB** ต่อไฟล์
- **ไม่จำกัดจำนวนไฟล์**
- List ไฟล์ที่แนบ + ปุ่ม X ลบทีละไฟล์
- Validation: type + size ก่อนแนบ

### Settings → Master → Sub-tab "รายชื่อพนักงาน" (ใหม่)

**Icon:** Users
**Table:**
| Column | Type |
|---|---|
| รหัสพนักงาน | text mono |
| ชื่อ-สกุล | bold |
| แผนก | chip code + name_th |
| ระดับ | position name |
| สถานะ | Active/Inactive badge |
| Action | Edit / Delete |

**Modal Add/Edit:**
- **รหัสพนักงาน** * (unique)
- **ชื่อ-นามสกุล** *
- **แผนก** — Dropdown จาก `masterApi.departments`
- **ระดับ** — Dropdown จาก `positionApi.list`
- **Active toggle**

**Endpoints frontend:**
- `staffApi.list()` — public dropdown
- `staffApi.listAll()` — admin (รวม inactive)
- `staffApi.create()` / `update()` / `delete()`

**Types:**
- `Staff` (id, employee_id, full_name, department, position, is_active)
- `StaffUpsert`

---

## ✅ Migration History (ตอนนี้)

| # | ชื่อ | สรุป |
|---|---|---|
| 001 | init | Users, departments, documents, ...|
| 002 | seed | Departments + document_types |
| 003 | project_fields | company_name, work_order, UAT/UAI, ... |
| 004 | settings | app_settings table |
| 005 | login_methods | Azure config |
| 006 | user_profile | avatar, employee_id |
| 007 | positions | Positions master + user.position_id |
| 008 | avatar | users.avatar_path |
| 009 | ack_per_user | Per-user unique |
| 010 | status_relabel | Standard/Modify/Add-on |
| 011 | ack_single_lock | 1 ack per doc lock |
| 012 | notifications | SSE notifications |
| 013 | local_login_toggle | Local enabled default |
| **014** | **staff_project_type** | **Staff master + project_type + revert status** |

---

## 🎬 การใช้งานหลังจบ session

### เข้าใช้งาน
```
Local dev:      https://localhost:3000/Document/login
LAN:            https://10.51.192.198:3000/Document/login
```

### Users ทดสอบ (Local)
| Username | Password | Role |
|---|---|---|
| `admin` | `admin123` | Admin |
| `thakorn.w@tigersoft.co.th` | (Azure) | Admin |

### Flow ทดสอบระบบใหม่

1. **Login admin** → Settings → Master → **รายชื่อพนักงาน**
2. คลิก **"+ เพิ่มพนักงาน"** ลงข้อมูล 2-3 คน (Isara N., Somchai K., ...)
3. เข้า Documents → **อัปโหลด**
4. เลือก **ประเภท: Standard**, กรอกบริษัท+WO, เลือก **ผู้รับผิดชอบ** จาก dropdown
5. เลือก **UAT: Pending** / **UAI: Passed** พร้อมวันที่
6. **ลากไฟล์ 2-3 อัน** เข้า drop zone (หรือคลิกเลือก)
7. **อัปโหลด** → redirect ไปหน้า Detail

---

## 📝 Known Issues / Todo (ยังไม่ทำ)

- [ ] Documents list page — ปรับ column ให้ตรง data ใหม่ (project_type badge, files_count)
- [ ] Detail page — ตัด Kind column ในตาราง Files (ตอนนี้ยังโชว์ UAT/UAI kind)
- [ ] เอกสารเก่า (Tiger DOC-2026-875392f1) — ผู้รับผิดชอบเป็น "-" ตามที่ตกลง
- [ ] Dashboard chart "ประเภทงาน" — ยัง query จาก uat_status (Pending/Passed/Failed แล้ว) ควรเปลี่ยนไปใช้ project_type แทน

---

## 🔧 Files Changed Today

### Backend
- `db/migrations/014_staff_project_type.up.sql` (ใหม่)
- `handlers/staff.go` (ใหม่)
- `handlers/document.go` (แก้ normStatus, normProjectType, Create, List, Detail)
- `models/models.go` (เพิ่ม Staff)
- `cmd/server/main.go` (register staff routes)
- `middleware/cors.go` (permissive LAN)

### Frontend
- `src/app/(protected)/documents/upload/page.tsx` (rewrite)
- `src/app/(protected)/settings/tabs/MasterTab.tsx` (เพิ่ม StaffPanel + StaffModal)
- `src/lib/api/endpoints.ts` (staffApi, Staff types, DocumentSummary.project_type)
- `src/lib/api/client.ts` (basePath support)
- `src/lib/auth/msal.ts` (basePath in redirect URI)
- `next.config.mjs` (basePath + rewrites)
- `.env.local` (NEXT_PUBLIC_BASE_PATH)
- `.gitignore` (add certificates)

### Root
- `start_dev.bat` (--experimental-https flag)

---

_Generated at end of session — 2026-07-02_
